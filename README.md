This is test project used to show my skills.

The C# API has file search, upload, copy, move and delete methods.

The default directory property is within the appsettings.json
"ApiSettings": {    
  "BaseDirectory": "C:\\WeatherApp"
}
